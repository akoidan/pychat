0. cat .ssh/id_rsa.pub | ssh -p 666 root@ip 'mkdir -p .ssh; cat >> .ssh/authorized_keys'
1. yum install nginx
2. yum install uWSGI
3. copy rootfs to /
4. `yum install pip3`
5. `yum install redis`
6. `yum install python34u-pip`
7. `yum install mysql-server, mysql-devel`

10. add file Chat/production.py , place SECRET_KEY there

11. echo "create database django CHARACTER SET utf8 COLLATE utf8_general_ci" | mysql
13 . chkconfig --add redis ; chkconfig redis on
14 . chkconfig --add mysqld ; chkconfig mysqld on
15 . chkconfig --add uwsgi ; chkconfig uwsgi on
16 . chkconfig --add tornado ; chkconfig tornado on
