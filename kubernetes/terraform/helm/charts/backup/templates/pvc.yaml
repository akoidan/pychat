apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-backup
  namespace: pychat
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.disk_size }}
  storageClassName: "" # Empty string must be explicitly set otherwise default StorageClass will be set
  volumeName: pv-pychat-backup
